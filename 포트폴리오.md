# Industry Insight Agent 포트폴리오 버전

## 1. 프로젝트 한 줄 소개

비정형 산업 뉴스 데이터를 자동 수집하고, LLM으로 분류·요약하여, 분석 가능한 구조화 데이터와 대시보드로 연결한 업무 자동화 시스템입니다.

## 2. 이 프로젝트를 어떻게 포지셔닝할 것인가

이 프로젝트는 단순 뉴스 분석 프로젝트가 아니라 아래 흐름을 구현한 자동화 시스템으로 설명하는 것이 가장 좋습니다.

`비정형 데이터 입력 -> 자동 수집/정제 -> LLM 처리 -> 구조화 저장 -> 분석/시각화 -> 의사결정 지원`

즉, 핵심은 "뉴스를 잘 분류했다"가 아니라 아래 3가지입니다.

- 비정형 데이터를 실무에서 쓸 수 있는 구조화 데이터로 변환했다
- LLM을 단발성 호출이 아니라 파이프라인 안에 넣었다
- 사람이 하던 반복 업무를 자동화 가능한 시스템으로 만들었다

## 2-1. 프로젝트 목표

이 프로젝트의 목표는 단순히 뉴스 데이터를 분류하는 것이 아니라,
비정형 텍스트 데이터를 자동으로 수집하고 분석해
실제 업무에 활용 가능한 구조화 데이터로 바꾸는 것이다.

구체적인 목표는 아래와 같다.

- 산업 뉴스 모니터링 업무를 자동화 가능한 형태로 바꾸기
- LLM을 이용해 뉴스 분류와 요약을 자동화하기
- 결과를 CSV와 대시보드 형태로 저장해 후속 분석에 활용 가능하게 만들기
- 사람이 읽어야만 하던 뉴스 텍스트를 의사결정 지원 데이터로 전환하기
- 향후 Agent, 알림, RPA로 확장 가능한 기본 구조를 설계하기

## 2-2. 활용 기술

이 프로젝트에서 활용한 핵심 기술은 아래와 같다.

- Python
  전체 파이프라인 구현
- pandas
  데이터 로딩, 정제, 구조화, 분석
- OpenAI API
  뉴스 분류와 요약용 LLM 처리
- Rule-based fallback
  API 미사용 또는 실패 시 동작하는 안정성 보완 로직
- RSS ingestion
  자동 수집 계층 구현
- Streamlit
  분석 결과 대시보드 UI
- matplotlib
  카테고리 분포 시각화 저장
- Git / GitHub
  버전 관리 및 포트폴리오 관리

## 2-3. 활용 데이터

이 프로젝트에서 활용하는 데이터는 아래처럼 나눌 수 있다.

### 원천 데이터

- `Financial.csv`
  금융 관련 뉴스/지표성 텍스트 데이터
- `Google_Daily_News.csv`
  일반 비즈니스/산업 뉴스 데이터

### 자동 수집 데이터

- `ingested_news.csv`
  live RSS 또는 local seed fallback을 통해 생성된 자동 수집 결과

### 분석 결과 데이터

- `result.csv`
  분류와 요약이 추가된 구조화 결과 데이터

### 데이터 안에서 중요한 필드

- `title`
- `content`
- `industry`
- `category`
- `summary`
- `source_name`
- `collection_method`
- `processed_at`

## 3. 햄프킹과의 연결 포인트

햄프킹에서 강조될 가능성이 높은 키워드와 본 프로젝트의 연결은 아래처럼 정리할 수 있습니다.

### AI Agent

- 입력 데이터를 받아 단계별로 처리하는 파이프라인을 설계했다
- 수집, 분류, 요약, 저장, 분석이라는 역할이 분리된 구조다
- 향후 알림, 액션 실행, RPA 연동까지 확장 가능한 형태로 만들었다

### RPA 자동화

- 사람이 매일 뉴스를 읽고 분류하고 요약하는 반복 업무를 자동화 대상으로 정의했다
- 자동 수집 계층과 결과 저장 구조를 넣어 후속 자동화에 바로 연결할 수 있게 했다
- 결과 CSV는 다른 시스템 입력값이나 보고 자동화의 중간 산출물로 활용할 수 있다

### 비정형 데이터 처리

- 뉴스 텍스트를 정규화하고 분류/요약해 구조화 데이터로 전환했다
- 단순 정형 데이터 분석이 아니라 텍스트 중심 데이터 처리 역량을 보여준다
- 분류 체계와 요약 결과를 통해 검색·분석·모니터링이 가능한 형태로 바꿨다

### 업무 프로세스 구조화

- 문제를 데이터 수집, 전처리, AI 처리, 저장, 분석, 시각화 단계로 나눠 설계했다
- 운영 관점에서 입력과 출력이 명확한 파이프라인을 만들었다
- fallback 경로를 둬서 API가 없거나 네트워크가 제한돼도 데모가 가능하다

## 4. 포트폴리오에서 강조해야 할 핵심 메시지

### 강조 1. "LLM 활용"보다 "LLM 내재화"

이 프로젝트는 단순히 프롬프트를 한 번 던진 것이 아니라, LLM을 실제 처리 단계 안에 내재화한 시스템이라는 점을 강조하는 것이 좋습니다.

### 강조 2. "분석 프로젝트"보다 "자동화 프로젝트"

이 프로젝트는 pandas 분석이 중심이 아니라, 사람이 하던 뉴스 모니터링 업무를 자동화 가능한 형태로 바꾼 것이 핵심입니다.

### 강조 3. "텍스트 처리"보다 "실무 활용 가능한 구조화"

뉴스 텍스트를 category, summary, source, processed_at 같은 필드로 저장해 후속 분석과 리포팅에 쓰일 수 있도록 만든 점을 강조하세요.

### 강조 4. "기능 구현"보다 "업무 설계 능력"

문제를 기능 단위가 아니라 업무 흐름 단위로 나눠 설계했다는 점이 중요합니다.

## 5. 시스템 설명 문구

면접이나 포트폴리오 소개에서 아래 문구를 그대로 써도 자연스럽습니다.

> 산업 뉴스 모니터링 업무를 자동화하기 위해, 뉴스 수집부터 LLM 분류·요약, 결과 저장, 트렌드 분석, 대시보드 시각화까지 이어지는 end-to-end 파이프라인을 구현했습니다.

짧게 말할 때는 이렇게 줄일 수 있습니다.

> 비정형 뉴스 데이터를 구조화해 의사결정에 연결하는 LLM 기반 자동화 시스템입니다.

## 6. 문제 정의를 더 실무적으로 말하는 방법

기존 방식:

- 분석가가 여러 소스의 뉴스를 직접 확인
- 제목과 본문을 읽고 주제를 수작업으로 분류
- 핵심 내용을 요약해 별도 문서나 시트에 정리

문제점:

- 데이터 양이 많아 누락 가능성이 높다
- 분류 기준이 사람마다 달라 일관성이 떨어진다
- 반복 작업이 많아 고부가가치 분석에 시간을 쓰기 어렵다

개선 방향:

- 수집 자동화
- LLM 기반 분류/요약 자동화
- 분석 가능한 구조로 저장
- 대시보드 기반 모니터링

## 7. 데모 시나리오

발표나 과제 제출에서는 아래 순서로 보여주면 좋습니다.

1. 자동 수집 또는 저장된 CSV를 입력으로 선택한다
2. 파이프라인 실행으로 뉴스가 정규화되고 분류/요약되는 과정을 보여준다
3. 생성된 `result.csv`에서 구조화된 필드를 보여준다
4. Streamlit에서 카테고리 분포, 산업별 비교, 키워드 분석을 보여준다
5. 사람이 하던 반복 업무가 어떻게 시스템화됐는지 설명한다

## 7-1. 어떤 시각화가 있고 왜 중요한가

이 프로젝트에서 사용할 수 있는 주요 시각화는 아래와 같다.

### 1. 카테고리 분포

- 어떤 유형의 뉴스가 가장 많이 발생하는지 빠르게 확인할 수 있다
- 시장, 기업, 제조, 공급망 같은 이슈의 비중을 직관적으로 보여준다
- 전체 흐름을 가장 빠르게 설명할 수 있는 대표 시각화다

### 2. 산업별 카테고리 비교

- 어떤 산업에서 어떤 이슈가 많이 발생하는지 비교할 수 있다
- 예를 들어 금융은 시장 이슈가 많고, 제조는 생산/공급망 이슈가 많다는 식의 패턴을 볼 수 있다
- 단순 집계보다 "산업별 특성 차이"를 보여주기 때문에 포트폴리오에서 중요하다

### 3. 키워드 분석

- 뉴스에서 반복적으로 등장하는 핵심 토픽을 파악할 수 있다
- 정량적으로 어떤 주제가 많이 언급되는지 보여준다
- 분류 결과를 보완하는 설명 자료로 유용하다

### 4. 수집 방식 분포

- live RSS, local seed fallback, csv load 같은 수집 경로를 보여줄 수 있다
- 이 프로젝트가 단순 분석이 아니라 자동화 파이프라인이라는 점을 설명할 때 중요하다
- "운영 관점"과 "안정성 설계"를 보여주는 시각화다

### 이 프로젝트에서 특히 중요한 시각화

가장 중요한 것은 아래 두 가지다.

- 카테고리 분포
  전체 뉴스 흐름과 이슈 집중도를 바로 보여줄 수 있기 때문
- 산업별 카테고리 비교
  단순 뉴스 모음이 아니라 산업 인사이트 분석 시스템이라는 점을 보여주기 때문

즉, 키워드 분석은 보조 설명 역할을 하고,
카테고리 분포와 산업별 비교가 포트폴리오 핵심 시각화라고 보면 된다.

## 8. 코드 기준으로 실제 강조 가능한 구현 포인트

- `src/data/collect_news.py`
  자동 수집 계층. live RSS 우선, 실패 시 local seed fallback 구조
- `src/pipeline.py`
  수집부터 분석까지 연결하는 메인 오케스트레이션
- `src/llm/classify_news.py`
  뉴스 카테고리 분류 엔진
- `src/llm/summarize_news.py`
  핵심 내용 요약 엔진
- `app.py`
  자동화 결과를 보여주는 운영형 대시보드

## 9. 햄프킹 맞춤 어필 문장

### 버전 A

비정형 텍스트 데이터를 단순히 분석하는 수준을 넘어, 실제 업무 프로세스 안에서 자동으로 수집·처리·저장·시각화되는 구조로 만들었다는 점이 이 프로젝트의 핵심입니다.

### 버전 B

LLM을 활용해 사람이 하던 뉴스 분류와 요약 업무를 자동화하고, 후속 데이터 활용이 가능하도록 구조화된 결과를 생성하는 파이프라인을 설계했습니다.

### 버전 C

AI 모델 호출 자체보다 중요한 것은 이를 업무 자동화 흐름 안에 배치하는 것이라고 생각했고, 이 프로젝트에서 그 구조를 end-to-end로 구현했습니다.

## 10. 면접에서 물어볼 가능성이 높은 질문과 답변 포인트

### 왜 이 프로젝트를 했나요

- 비정형 데이터 처리와 업무 자동화를 같이 보여줄 수 있는 주제였기 때문
- 단순 분석이 아니라 실무형 시스템 설계 역량을 보여주고 싶었기 때문

### 단순 뉴스 요약기와 무엇이 다른가요

- 수집, 정제, 분류, 요약, 저장, 분석, UI까지 이어지는 전체 파이프라인이라는 점
- 결과를 구조화해 후속 분석과 자동화에 연결할 수 있다는 점

### 햄프킹 업무와 어떤 관련이 있나요

- 비정형 데이터 처리
- 반복 업무 자동화
- AI 기능을 실제 시스템으로 연결하는 설계 경험

## 10-1. 프로젝트에서 찾을 수 있는 질문과 답변

아래 질문들은 실제 포트폴리오 발표나 면접에서 바로 연결할 수 있는 질문들이다.

### 이 프로젝트는 단순 뉴스 분석 프로젝트와 무엇이 다른가요

답변:
단순히 뉴스를 분류한 것이 아니라, 수집부터 정제, 분류, 요약, 저장, 분석, 시각화까지 이어지는 자동화 파이프라인으로 만들었다는 점이 다르다.

### 왜 결과를 구조화 데이터로 저장했나요

답변:
뉴스 원문만 있으면 다시 사람이 읽어야 하기 때문이다. category, summary, source, processed_at 같은 필드를 추가하면 후속 분석, 리포트 자동화, RPA 입력값으로 재사용할 수 있다.

### 이 프로젝트에서 가장 중요한 기술적 선택은 무엇이었나요

답변:
LLM 사용 자체보다 fallback 구조를 넣은 점이다. live RSS 실패 시 local seed fallback, OpenAI 미사용 시 rule-based fallback을 두어 전체 파이프라인이 끊기지 않게 설계했다.

### 왜 시각화가 필요한가요

답변:
분류와 요약 결과를 사람이 빠르게 해석할 수 있도록 만들기 위해서다. 데이터가 구조화되어도 한눈에 패턴이 보이지 않으면 활용성이 낮기 때문에, 카테고리 분포와 산업별 비교 시각화가 중요하다.

### 이 프로젝트를 햄프킹 관점에서 한 문장으로 표현하면 무엇인가요

답변:
비정형 텍스트 데이터를 자동으로 수집·처리·구조화하여 후속 업무 자동화와 의사결정 지원에 연결하는 LLM 기반 시스템이다.

## 11. 앞으로 더 확장하면 좋은 방향

- 실시간 뉴스 스트리밍
- 감성 분석 추가
- 중요 이벤트 알림 자동화
- RPA 또는 사내 툴 입력 연동
- 주간 리포트 자동 생성

## 12. 최종 요약

이 프로젝트는 "뉴스 분석"이 아니라 "비정형 데이터 기반 업무 자동화 시스템"으로 설명할 때 가장 강합니다.

햄프킹 관점에서 보면 특히 아래 역량을 보여줍니다.

- LLM 활용 능력
- 자동화 파이프라인 설계 능력
- 비정형 데이터 처리 능력
- 분석 결과를 실제 사용 가능한 형태로 연결하는 능력
